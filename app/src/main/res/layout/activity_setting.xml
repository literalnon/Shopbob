<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="dmproject.moviebuff.Activity.SettingActivity"
    android:background="@color/colorBackStart">

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Music"
        android:id="@+id/swch_music"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:checked="true"
        android:nestedScrollingEnabled="true" />

    <Button
        android:id="@+id/button_start"
        android:text="@string/play"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:gravity="center"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:background="@drawable/button_play"
        android:shadowColor="#A80808"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="0"
        android:onClick="clickToStart"
        android:layout_marginBottom="82dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:text="Tanya"
        android:ems="10"
        android:id="@+id/etName"
        android:layout_below="@+id/swch_music"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/swch_music" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:id="@+id/tvPlayersResults"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/button_start"
        android:layout_alignEnd="@+id/swch_music"
        android:layout_below="@+id/etName" />

</RelativeLayout>
